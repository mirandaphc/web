- [ ] Navegación móvil: perfilar por qué la cabecera fija y el menú horizontal siguen sintiéndose lentos (carga a trompicones en iPhone); revisar recalculados de layout y listeners restantes.
- [ ] Medir repaints y fluidez al cargar para asegurarse de que las mezclas de color, el render inicial y `IntersectionObserver` no bloquean la renderización en móviles.
- [ ] Confirmar que favicon/navicon se muestran correctamente en desktop, iOS y Android.
- [x] Panel About: centrar contenido en el overlay, cambiar el botón por una “x” simple en la esquina superior izquierda respetando `safe-area`, añadir footer `web: meowrhino` a ~5dvh del borde inferior y bloquear el scroll principal mientras esté abierto.
- [ ] Refactorizar el sistema de carga de datos con un helper `fetchJson` que valide `response.ok` y centralice manejo de errores.
- [ ] Extraer utilidades para construir la estructura de proyecto (`buildProjectInfo`, `buildMediaGallery`) y así evitar duplicar lógica entre `renderProjects` y `updateProjectsContent`.
- [ ] Revisar la heurística de color dinámico: fijar un umbral mínimo (p. ej. 0.45) o aplicar un test de contraste para no oscurecer fondos ya oscuros; documentar `nota_de_curt` como porcentaje 0–100.
- [ ] Añadir soporte para `prefers-reduced-motion` y desactivar `scroll-behavior: smooth` y transiciones cuando el sistema lo indique.
- [ ] Gestionar el estado del About cuando no haya JSON (p. ej. deshabilitar botón o mostrar mensaje) mediante una función `toggleAboutAvailability`.
- [x] Ajustar el tratamiento de imágenes con `size=100`: evaluar si conviene permitir que llenen el hueco (aunque pixeladas) o mantener el ancho natural con opción para alinear a la izquierda.
- [ ] Validación final: test manual en escritorio (scroll, cambio de idioma, apertura/cierre de About, menú horizontal) y en móvil real (sticky, animaciones, centrado del menú, overlay del About, cierre con ESC/botón, bloqueo de scroll).
